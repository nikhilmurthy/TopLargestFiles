# TopLargestFiles
This PowerShell script returns top 10 largest files from a user's cloud storage root directory

For those downloading this file, please run the below cmdlet in the same directory as the PowerShell script to avoid Runtime Exceptions:

Unblock-File -Path .\Top10LargestCloudFiles.ps1
